[
  {
    "name": "/price",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Query current price: accepts a single ticker (typed) or no argument to return all tracked coins"
            }
          }
        ],
        "send": {
          "text": "/price"
        }
      }
    ]
  }
]
